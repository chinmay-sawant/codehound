name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: test (${{ matrix.os }} / ${{ matrix.features }})
    runs-on: ${{ matrix.os }}
    env:
      CARGO_TERM_COLOR: always
      RUSTFLAGS: -D warnings
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        features:
          - default
          - go
          # Opt-in Python (ADR 0005): build with go+python so Go tests still run.
          - go,python
    steps:
      - uses: ./.github/actions/rust-toolchain-cache
        with:
          components: clippy, rustfmt
          features: ${{ matrix.features }}
      - name: Build
        run: |
          if [ "${{ matrix.features }}" = "default" ]; then
            cargo build --all-targets
          else
            cargo build --all-targets --no-default-features --features ${{ matrix.features }}
          fi
      - name: Test
        run: |
          if [ "${{ matrix.features }}" = "default" ]; then
            cargo test --all-targets
          else
            cargo test --all-targets --no-default-features --features ${{ matrix.features }}
          fi

  lint:
    name: lint
    runs-on: ubuntu-latest
    env:
      CARGO_TERM_COLOR: always
      RUSTFLAGS: -D warnings
    steps:
      - uses: ./.github/actions/rust-toolchain-cache
        with:
          components: clippy, rustfmt
          features: lint
      - name: rustfmt
        run: cargo fmt --all -- --check
      - name: No production unwrap/expect
        run: bash scripts/check_no_prod_expect.sh
      - name: No unbounded Box::leak on finding path (allowlist)
        run: bash scripts/check_no_box_leak.sh
      - name: Module size policy (src ≤ 400 lines)
        run: bash scripts/check_module_size.sh
      - name: clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

  msrv:
    name: MSRV (1.85)
    runs-on: ubuntu-latest
    env:
      CARGO_TERM_COLOR: always
      RUSTFLAGS: -D warnings
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.100
      - name: Test on MSRV
        run: cargo test --all-targets

  audit:
    name: cargo audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      - name: Audit dependencies
        run: cargo audit

  canary:
    name: canary budgets
    runs-on: ubuntu-latest
    env:
      CARGO_TERM_COLOR: always
      RUSTFLAGS: -D warnings
    steps:
      - uses: ./.github/actions/rust-toolchain-cache
        with:
          features: default
      - name: Build release binary
        run: cargo build --release --bin codehound
      - name: Run canary finding-count budgets
        env:
          CODEHOUND_BIN: ${{ github.workspace }}/target/release/codehound
        run: bash scripts/canary/run_canaries.sh

  bench:
    name: bench
    runs-on: ubuntu-latest
    env:
      CARGO_TERM_COLOR: always
      RUSTFLAGS: -D warnings
    steps:
      - uses: ./.github/actions/rust-toolchain-cache
        with:
          features: bench
      - name: Run criterion bench
        run: cargo bench --bench scan_throughput -- --output-format bencher 2>&1 | tee bench_output.txt
      - name: Check perf budget
        run: bash scripts/check_bench_budget.sh
      - name: Run incremental bench
        run: cargo bench --bench incremental_scan -- --output-format bencher 2>&1 | tee bench_incremental_output.txt
      - name: Check incremental bench budget
        run: bash scripts/check_incremental_bench_budget.sh
