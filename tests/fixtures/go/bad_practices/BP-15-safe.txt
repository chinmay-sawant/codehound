# BP-15 negative: sync.Once.Do is not recursive
lang: go
file: BP-15-safe.go
variant: stdlib
---
package sample

import "sync"

var once sync.Once

func Init() {
	once.Do(func() {
		setup()
	})
}

func setup() {}
