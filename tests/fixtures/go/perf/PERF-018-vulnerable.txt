# PERF-018 positive: append(result, items...) copies the whole slice
lang: go
file: PERF-018-vulnerable.go
---
package sample

func processItems(items []int) []int {
	result := make([]int, 0, len(items))
	result = append(result, items...)
	return result
}
