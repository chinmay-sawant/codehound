# PERF-028 negative: sync.Mutex is a package-scope singleton
lang: go
file: PERF-028-safe.go
---
package sample

import (
	"net/http"
	"sync"

	"github.com/gin-gonic/gin"
)

var mu sync.Mutex

var counter int

func Increment(c *gin.Context) {
	mu.Lock()
	counter++
	mu.Unlock()
	c.JSON(http.StatusOK, gin.H{"n": counter})
}
