# Mandatory fixture manifest — paths are .txt text fixtures only.
# Sources are materialized under target/slopguard-fixtures/ before analysis.

[[fixture]]
lang = "go"
path = "tests/fixtures/go/sample.txt"
required_rules = ["SLOP001"]

[[fixture]]
lang = "python"
path = "tests/fixtures/python/sample.txt"
required_rules = ["SLOP101"]
