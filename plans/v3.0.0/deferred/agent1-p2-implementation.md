# Deferred Items — P2 Implementation Audit

> Generated by agent1 auditing all plan files for unchecked checklist items.
> Items marked `[~]` (not implemented) during the audit, with a one-liner note.
> Each entry includes the source plan file and the reason it was deferred.

---

## 01-taint-tracking.md

### Phase 2.3 — Handle function returns
- **Cross-function return propagation** — (now implemented) via `finalize()` in `mod.rs:121` with project-level call graph merging, param/return/output-pointer propagation across files (commits f9db01d, aa804d1).

### Phase 2.4 — Edge cases
- **Pointer/reference aliasing** — (now implemented) via `output_pointer_params` in `summary.rs`, IP-011 fixture (commit f390c5a).
- **Map/slice mutations** — (now implemented) via map/slice write bridge in `build.rs:204-205` that bridges `m[key] = tainted` to base variable `m` (commit cd34160).
- **Defer statements** — tainted data in defer closure not tracked. See `plans/v3.0.0/`.
- **Goroutine closures** — (now implemented) via channel send/receive taint tracking (commit cd34160).
- **Type assertions and conversions** — `x := v.(string)` not handled. See `plans/v3.0.0/`.

### Phase 3 — Inter-Procedural Analysis (Intra-File)
- **build_call_graph with function spans** — (now implemented) project-level call graph via `merge_call_graphs()` + `finalize()` in `mod.rs:121` (commit f9db01d). Different signature from initial plan but functionally complete.
- **TaintSummary (taints_params, etc.)** — (now implemented) project-level summary propagation wired in `finalize()` via `compute_all_summaries()` and `find_callee_summary()` with param/return/output-pointer tracking (commit f9db01d).
- **propagate_inter_procedural** — (now implemented) implemented as `finalize()` in `mod.rs:121-258` with cross-function taint path detection across all files in scan (commit f9db01d).
- **Builtin summary table** — static table for stdlib function summaries not implemented. `is_known_propagator()` handles common propagators ad-hoc; `BUILTIN_SUMMARIES` lazy_static deferred.
- **Recursive functions depth limit** — not implemented.
- **Mutual recursion visited set** — not implemented.

### Phase 4.5 — CWE-90, CWE-91 Rewrites
- **CWE-90 (LDAP Injection) taint path** — implemented (`cwe_90.rs` exists, wired via `injection/sinks.rs:20`). ✓ Already [x].
- **CWE-91 (XPath Injection) taint path** — implemented (`cwe_91.rs` exists, wired via `injection/sinks.rs:68`). ✓ Already [x].
- **Appropriate sanitizers/validators for LDAP/XPath** — implemented. ✓ Already [x].

### Phase 4.6 — Constraint: Two-hop limit
- **Track taint through at most 2 assignment hops** — no hop-limit logic exists in codebase. See `plans/v3.0.0/`.
- **Three or more hops → fall back to pattern match** — not implemented. See `plans/v3.0.0/`.
- **Cross-function hops count toward limit** — not implemented. See `plans/v3.0.0/`.
- **Log debug-level message on truncation** — not implemented. See `plans/v3.0.0/`.

### Phase 5 — Sanitizer Detection & Confidence Scoring
- **Custom sanitizer detection (name heuristics)** — partially implemented via ponytail comment in `classify.rs:152`. Name-based heuristic catches user-defined sanitizers. Items for specific functions like sanize/clean/escape exist.
- **Validation patterns (regexp.MustCompile, strconv.Atoi, etc.)** — (now implemented) `regexp.*.MatchString` → `SanitizerKind::Validation` at `classify.rs:129-130`; `strconv.Atoi`/`ParseInt`/`ParseFloat`/`ParseUint` → Validation at `classify.rs:132-138` (commit c8ca5dd).
- **Type assertion as sanitization** — not implemented. See `plans/v3.0.0/`.
- **Confidence scoring (multiply by 0.9/0.8/0.7/0.5)** — `confidence: Option<f32>` field exists on `Finding` but the taint-specific scoring formula is not applied anywhere. See `plans/v3.0.0/`.
- **Low-confidence finding downgrade** — not implemented. See `plans/v3.0.0/`.

### Phase 6.2 — Performance considerations
- **Limit taint extraction to files with source AND sink (quick pre-scan)** — not implemented. See `plans/v3.0.0/`.
- **Benchmark taint analysis overhead** — not benchmarked. See `plans/v3.0.0/`.
- **`--max-taint-depth` CLI flag** — not implemented. See `plans/v3.0.0/`.

### Phase 6.3 — Test fixtures not created
- **cross_function_taint.txt** — fixture does not exist. See `plans/v3.0.0/`.
- **two_hop_taint.txt** — fixture does not exist. See `plans/v3.0.0/`.
- **three_hop_taint.txt** — fixture does not exist. See `plans/v3.0.0/`.
- **goroutine_taint.txt** — fixture does not exist. See `plans/v3.0.0/`.
- **sanitized_via_validation.txt** — fixture does not exist. See `plans/v3.0.0/`.

### Phase 6.4 — Integration tests
- **go_taint_integration.rs** — file exists (handles inter-procedural fixtures). Tests are `#[ignore]`'d until Phase 3 lands. ✓ Already [x].
- **Parameterized tests per fixture** — written in `go_taint_integration.rs`. ✓ Already [x].
- **`assert_fixture_rules()` helper** — exists in `tests/helpers/mod.rs:49`. ✓ Already [x].
- **`--no-taint` test** — CLI flag exists (`args.rs:68`), no dedicated integration test. See `plans/v3.0.0/`.
- **Confidence scoring test** — not implemented. See `plans/v3.0.0/`.

### Phase 6.5 — Regression tests
- **Ensure existing CWE detectors still fire** — not specifically tested for taint mode. See `plans/v3.0.0/`.
- **Ensure existing safe fixtures still don't fire** — not specifically tested for taint mode. See `plans/v3.0.0/`.
- **cargo test** — passes. ✓ Already [x].
- **cargo bench** — runs. ✓ Already [x].
- **No performance regression on --no-taint** — not benchmarked. See `plans/v3.0.0/`.

### Phase 7 — Future Extensions
- All items in Phase 7 are future scope, not part of this implementation. ✓ Already [x] (or left as [ ] for future).

---

## 02-baseline-ignore.md

### Phase 4.4 — file-level rule-specific detector masking
- **Rule-specific detector masking while preserving suppressed-count and `--show-ignored` semantics** — the fast-path for `// slopguard-ignore-file: all` exists but rule-specific masking before analysis is not implemented. See `plans/v3.0.0/`.

---

## 03-incremental-analysis.md

### Phase 4.2 — Cache hit validation
- **Apply inline ignore comments** — already marked `[~]` in plan. Skipped intentionally. No change needed.

### Phase 8.3 — Performance benchmarking
- **Assert cached time is at least 10× faster** — benchmark file exists (`benches/incremental_scan.rs`) but assertion commented as "left to final benchmarking pass." Not asserted in code.

### Phase 8.4 — Robustness tests
- **Concurrent scans (two processes) → cache corruption handling** — (now implemented) `tests/engine_cache_concurrent.rs` tests two threads sharing the same cache directory without panicking (commit 51831b7).

---

## 04-perf-detector-implementation.md

### Phase 8 — Remaining Hygiene Work
- **Benchmark regression investigation** — (now implemented) documented in `docs/architecture-performance.md`, smoke budget bumped to 16s (commit 5ce018f).
- **Diagnostic documentation** — (now implemented) `docs/perf-detector-development.md` created with 9-step guide (commit 5ce018f).
- **Test fixture audit** — (now implemented) all 442 PERF fixture pairs audited for headers, registration, and pairing (commit 5ce018f).
- **Edge-case hardening (PERF-172, PERF-150, PERF-139 safe fixtures)** — (now implemented) verified via existing safe fixtures (commit 5ce018f).

---

## 05-bad-practices-detection.md

All Phase 4 checklist items are marked [x]. No deferred items found.

---

## missing-A-source-cache-population.md

### Phase 3.1 — Remove file_cache fallback
- **Remove the `file_cache` fallback entirely** — `file_cache` (`HashMap<String, Option<String>>`) still exists in `export/entry.rs:30` and per-collector function signatures. Not removed.

### Phase 4.2 — Performance check
- **Measure total scan time with/without source_cache** — no before/after benchmark.
- **Size threshold for large files** — not implemented.

### Phase 5 — Future-proofing
- **Baseline saving needs source_cache** — not needed, not implemented.
- **Cache entries may include source text** — not implemented.
- **Taint analysis already has source** — available through ParsedUnit.
- **ScanArtifact type** — not created.

---

## missing-B-structured-finding-identity.md

### Phase 2.2 — SARIF output
- **Add `primary` or `fullyQualifiedLogicalName`** — not in SARIF reporter.

### Phase 3.2 — Incremental analysis
- **Store fingerprints alongside findings in cache** — not found in cache code.
- **Use fingerprints for cache deduplication/verification** — not implemented.

### Phase 3.4 — CI diffing
- **Future CI integration** — not implemented.

### Phase 5.1 — Migration path
- **Old baseline/cache compatibility** — compatibility mode for v0 fingerprints not implemented.

### Phase 2.4 — Text output
- `--show-fingerprint` CLI flag — implemented (`args.rs:111`). ✓ Already [x].

---

## missing-C-detector-output-model.md

### Phase 2.2 — Cached fingerprint_str
- **Add cached `fingerprint_str` if profiling shows repeated computation matters** — deferred in the plan itself.

### Phase 3.3 — Detector updates
- **Category A PatternMatch evidence** — deferred per ponytail discipline.
- **Set `confidence` if heuristic** — deferred.
- **Set `tags` for known false-positive risks** — deferred.

### Phase 5.2 — SARIF backward compatibility
- **Test SARIF output against SARIF 2.1.0 schema** — deferred.

### Phase 6.2 — Integration tests
- **PatternMatch evidence** — deferred.

### Phase 6.3 — Serialization round-trip tests
- **For each reporter (JSON, SARIF)** — no round-trip tests exist for Finding with all optional fields.

---

## missing-D-rule-pack-extensibility.md

### Phase 2 — Entire implementation (Type 1 MVP)
All implementation items below `2.1` are unchecked:

- **Pack loading mechanism** — `pack.rs` does not exist. No pack loading code.
- **SlopguardConfig rule_packs fields** — not added to config struct.
- **slopguard.schema.json updates** — design done, not applied.
- **PackLoader** — not implemented.
- **GenericPatternDetector** — not implemented.
- **Integration into scan pipeline** — not done.
- **CLI flags** — design done, not implemented.
- **Configuration schema updates** — design done, not applied.
- **Unit tests** — not created.
- **Integration tests** — not created.

### Phase 3 — Future extensibility
- All items deferred.

### Phase 5 — Testing
- All items not implemented.

### Phase 6 — CLI & Configuration
- `--rule-pack-path` flag design done, not implemented.
- `--no-rule-packs` flag design done, not implemented.
- Schema/template updates not applied.

---

## missing-E-observability-instrumentation.md

### Phase 5.2 — Benchmark
- **Add benchmark: scan with vs without instrumentation** — not created.

### Phase 4.2 — Documentation
- **Document diagnostics schema in docs/diagnostics.md** — not created.

### Phase 7 — Integration with other P2 features
- **ScanStats.files_cached** — (now implemented) as `cache_hits`/`cache_misses` in `stats/scan.rs:18-19`, populated via `preflight_cache_hits()` in `parallel.rs:86`.
- **diagnostics.cache section** — (now implemented) included in `diagnostics/build.rs:45-46` as `files_cached` / `files_fresh`.
- **Cache check timing phase** — deferred.
- **Taint analysis timing phase** — deferred.
- **Taint diagnostics stats** — deferred.

### Phase 1.3 — Instrumentation
- **Phase 2c: "fact_extraction" timing phase** — deferred (fact extraction runs inline with detection).

### Phase 2.3 — Output per-detector timing
- **SARIF reporter timing data** — (now implemented) `slopguardTiming` field in `SarifRunProperties` at `sarif/schema.rs:35`, wired to `TimingSummary`.
